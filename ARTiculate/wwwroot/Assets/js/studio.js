function openFolder(ev) {
    let folderContainer = ev.parentNode.parentNode
    let folder = folderContainer.children[0]
    let arrow = folderContainer.children[1]
    let folderItems = folderContainer.children[2]
    console.dir(folderContainer)

    ////Lägger på klass med margins
    //folder.classList.Add()
}
